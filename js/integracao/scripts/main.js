(() => {
    // localizando e adicionando um evento ao botão
    let helloButton = document.getElementById("hello");

    helloButton.addEventListener("click", () => {
        window.alert("Hello ser vivo");
    });

    // Pegando texto do formulario
    
})();